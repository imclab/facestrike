FaceStrike

to build a project just run ./buildgame.sh. This script will:

* concat and obfuscate all the js files in src
* copy the html files
* copy (recursively) all the contents of the assets folder

